import { Box } from "@chakra-ui/react";
import { useState } from "react";

export default function PickCity() {
    return (
        <Box>

        </Box>
    );
};

// onChange={(e) => setCity(e.target.value)}
// onClick={()=>{
//     if(city==="") alert("Please select a valid city!");
//     else navigate("/pickupcity");
// }}